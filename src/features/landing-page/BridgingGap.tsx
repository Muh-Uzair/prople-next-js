import React from "react";

const BridgingGap: React.FC = () => {
  // VARS

  // FUNCTIONS

  // JSX
  return (
    <section className="bg-primary dark:bg-background flex w-full items-center justify-center">
      <div className="tab:px-[50px] tab:py-[50px] dark:bg-background max-w-[1200px] px-[20px] py-[30px] text-center">
        <div>
          <span className="laptopS:text-[35px] text-[30px] font-black text-white">
            Bridging the gap between building managers, tenants, and owners
          </span>
        </div>
        <div className="mt-[20px]">
          <span className="text-[18px] text-white">
            PROPLE simplifies multipurpose building operations by connecting
            everyone on one smart platform — for rent tracking, facility
            management, and seamless communication.
          </span>
        </div>
      </div>
    </section>
  );
};

export default BridgingGap;
