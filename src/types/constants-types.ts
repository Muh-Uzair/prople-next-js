import { LucideIcon } from "lucide-react";

export interface IBuildingManagerNavItems {
  navLabel: string;
  navIcon: LucideIcon;
  navUrl: string;
}

export interface IJwtObject {
  name: string;
  value: string;
}
